## 2024-04-26 - Add empty state to ListView
**Learning:** Using `listView.emptyView` in Android provides a built-in way to show an empty state when a list is empty, improving user guidance.
**Action:** Always implement an `emptyView` for dynamic `ListView`s to provide user guidance when the list is initially empty.
