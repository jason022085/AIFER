## 2026-04-14 - Android ListView Empty State
**Learning:** For dynamic ListViews in this application, an empty view provides essential context to the user when no actions have been taken yet.
**Action:** Always implement an `emptyView` (via `listView.emptyView = emptyView`) for dynamic ListViews to ensure users are not presented with a confusing blank area.
